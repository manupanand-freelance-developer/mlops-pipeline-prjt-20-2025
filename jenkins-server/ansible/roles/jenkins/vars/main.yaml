secrets: "{{lookup('hashi_vault','secret=tools/data/jenkins token={{vault_token}} url=https://vault.manupanand.click:8200 validate_certs=False)}}"
# secrets: "{{ lookup('hashi_vault',
#                     'secret=tools/data/jenkins token=' + vault_token + ' url=https://vault.manu:8200 validate_certs=False') }}"
